<!-- Generated by dt-spec@{{PLUGIN_VERSION}} at {{GENERATED_AT}} -->
<!-- status: {{STATUS}} -->
<!-- sources: {{SOURCES}} -->
<!-- specVersion: 1.1 -->

# Design: {{PAGE_NAME}}

## 아키텍처
{{ARCHITECTURE}}

## 데이터 모델 / API 계약
{{DATA_MODELS}}

## 컴포넌트 구조 (Figma 디자인 분석 기반)
{{COMPONENT_STRUCTURE}}

## 에셋 매니페스트
{{ASSET_MANIFEST}}

> 위 에셋은 `docs/specs/assets/<page>/`에 저장된 실제 파일이다(Figma `download_assets` 수거). 구현 단계가 이 경로로 실제 `<img src>`/SVG import를 배선한다 — 플레이스홀더 금지.

> 상세 레이아웃·스타일(inline style + placeholder)은 `layout-skeleton.md`를 참조한다.
> 전역 디자인 토큰(색/타이포/간격/radius/shadow)은 `docs/project-context.md` `## 디자인 토큰`을 참조한다.
> 물리적 파일/폴더 구조와 5-layer 배치는 이 문서에서 정하지 않는다 — 후속 scaffold 단계(`/dt-scaffold`)가 `dt-frontend-architecture` 룰에 따라 유일하게 결정한다.

## 시퀀스
{{SEQUENCES}}

## 요구사항 매핑 (FR ↔ 담당 책임)
{{REQUIREMENT_MAPPING}}
