// 재실행 시 이 파일은 다시 생성되지 않는다 — 인벤토리에 새 needs 가 생기면 states 에 TODO 줄만 추가되고, 나머지는 그대로 남는다.
// docs/guide/capture.config.mjs — /dt-guide 가 스캐폴딩한 캡처 레시피. 상태 준비 함수를 채우면 다음 판부터 그대로 다시 찍힌다.
// 계정·비밀번호는 여기에 쓰지 않는다 — process.env.GUIDE_TEST_* 로 받는다(.env.example 에 키 이름만).
export default {
  start: {{START}},
  viewport: { width: 1440, height: 900 },
  // 상태 준비 — inventory.screens[].capture.needs 의 각 이름에 대응. TODO 를 채운다(비워 두면 그 needs 를 요구하는 화면은 skipped).
  states: {
{{STATES}}
  },
  // 화면별 오버라이드(선택): beforeShot({ page, driver }) · selector · fullPage · redact
  // electron: view:/window: 라우트는 자동 이동이 안 된다 — 그 화면은 states 나 beforeShot 에서 직접 연다. fullPage 는 web 에서만.
  //
  // redact: [{ selector, text }] — 찍기 **직전에** 그 요소의 글자를 바꾼다.
  // 로그인한 계정·실제 파일 경로처럼 화면에 살아 있는 값이 공개 PDF 로 나가는 것을 막는다.
  // 게이트 G6 가 찍은 화면의 글자에서 이메일·홈 경로·API 키 모양·사내 표지를 찾아 빌드를 막으니,
  // 걸리면 여기에 한 줄 적는 것이 표준 대처다. 제품이 일부러 보여 주는 예시 값이면
  // .dt-guide.json 의 allowInShots 에 적어 통과시킨다.
  //   'SCR-settings': { redact: [{ selector: '[data-identity]', text: '사용자 계정 · 조직' }] },
  screens: {
{{SCREENS}}
  },
};
