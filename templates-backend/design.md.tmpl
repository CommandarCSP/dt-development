<!-- Generated by dt-spec@{{PLUGIN_VERSION}} at {{GENERATED_AT}} -->
<!-- status: {{STATUS}} -->
<!-- sources: {{SOURCES}} -->
<!-- specVersion: 1.1 -->

# Design: {{RESOURCE_NAME}}

## 아키텍처 (NestJS 모듈 책임 맵)
(어떤 Controller/Service/Repository가 어떤 책임을 지는지 — 논리 수준)
{{ARCHITECTURE}}

## 데이터 모델 / API 계약
(엔드포인트 × method × 상태코드 × 요청/응답 스키마. Prisma 모델 개요)
{{DATA_MODELS}}

> 결정론적 엔드포인트/스키마 표와 Prisma 스케치는 `api-contract.md` 참조.
> 전역 규약(에러 봉투/페이지네이션/인증)은 `docs/project-context.md` 참조.
> 물리적 파일/폴더 배치와 계층 매핑은 이 문서에서 정하지 않는다 — 후속 `/dt-be-scaffold`가 `dt-backend-architecture` 룰에 따라 결정한다.

## 인덱스 & 페이지네이션 전략
(주요 조회 패턴 → 필요한 인덱스, 목록 엔드포인트별 페이지네이션 방식. 전역 표준 형태는 `docs/project-context.md` 참조)
- **인덱스**: FK/조회 조건/정렬 컬럼별로 필요한 인덱스와 근거(어떤 쿼리 경로를 받치는지). 합성 유니크가 커버하는 조회는 명시.
- **페이지네이션**: 목록 엔드포인트별로 cursor / offset / 비페이지네이션 중 선택과 근거(무한 증가 목록 → cursor 권장).
{{INDEX_PAGINATION}}

## 시퀀스
(성공/검증실패/인증실패/충돌 분기, 트랜잭션 경계, 외부 호출)
{{SEQUENCES}}

## 요구사항 매핑 (FR ↔ 담당 책임)
{{REQUIREMENT_MAPPING}}
