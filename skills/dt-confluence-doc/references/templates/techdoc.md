# 기술 문서/위키 템플릿 (type: techdoc)

라벨: tech-doc. 구조:

1. **개요** — 무엇을/왜 (1문단)
2. **배경·맥락** — 필요 시
3. **설계·동작** — 산문 + (구조/흐름이 복잡하면) Mermaid 코드블록 `<pre><code class="language-mermaid">`
4. **결정 사항(ADR 스타일)** — 결정 리스트 또는 표 [결정 | 이유 | 대안]
5. **코드 예시** — `<pre><code class="language-ts">` (실제 동작 코드, 최소 예시)
6. **참고** — 링크(`<a href>` smart link 가능)

소스: git/세션/수동. 시각 요소는 visual-guideline 기준 충족 시에만.
